apiVersion: v1
kind: Service
metadata:
  name: calibre-web-automated
  labels:
    app.service: calibre-web-automated
spec:
  ports:
    - name: http
      port: {{ .Values.calibre-web-automated.service.port }}
      targetPort: {{ .Values.calibre-web-automated.env.CWA_PORT_OVERRIDE }}
  selector:
    app.service: calibre-web-automated
