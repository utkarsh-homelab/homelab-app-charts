{{- if .Values.calibre-web-automated.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: calibre-web-automated
  labels:
    app.service: calibre-web-automated
  {{- with .Values.calibre-web-automated.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.calibre-web-automated.ingress.ingressClassName }}
  {{- if .Values.calibre-web-automated.ingress.tls }}
  tls:
    {{- range .Values.calibre-web-automated.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.calibre-web-automated.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: Prefix
            backend:
              service:
                name: calibre-web-automated
                port:
                  number: {{ $.Values.calibre-web-automated.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
