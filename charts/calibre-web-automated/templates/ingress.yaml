{{- if .Values.calibre_web_automated.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: calibre-web-automated
  labels:
    app.service: calibre-web-automated
  {{- with .Values.calibre_web_automated.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.calibre_web_automated.ingress.ingressClassName }}
  {{- if .Values.calibre_web_automated.ingress.tls }}
  tls:
    {{- range .Values.calibre_web_automated.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.calibre_web_automated.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: Prefix
            backend:
              service:
                name: calibre-web-automated
                port:
                  number: {{ $.Values.calibre_web_automated.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
